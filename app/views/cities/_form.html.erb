<%= form_for(@city) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.collection_select :country_id, @countries, :id, :name, include_blank: 'Choose a Country' %><br>

  <%= f.submit %>
<% end %>